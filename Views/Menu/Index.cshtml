@model List<Ceng382_25_26_202311031.Models.MenuItem>

@{
    ViewData["Title"] = "Menus";
}

<section class="page-header">
    <h1 class="page-title">Menus</h1>
    <p class="page-subtitle">Browse available meals and add them to your cart.</p>
</section>

<div class="menu-grid">
    @foreach (var item in Model)
    {
        <div class="menu-card">
            <img class="menu-image" src="@(string.IsNullOrWhiteSpace(item.ImageUrl) ? "/images/food-hero-1.jpg" : item.ImageUrl)" alt="@item.Name" />

            <div class="menu-card-body">
                <div class="menu-top">
                    <h3>@item.Name</h3>
                    <span class="price-badge">@item.Price ₺</span>
                </div>

                <p class="menu-caterer"><strong>Caterer:</strong> @item.CatererName</p>
                <p class="menu-description">@item.Description</p>

                <form asp-controller="Cart" asp-action="AddToCart" method="post">
                    <input type="hidden" name="menuItemId" value="@item.Id" />
                    <button type="submit" class="menu-btn">Add to Cart</button>
                </form>
            </div>
        </div>
    }
</div>